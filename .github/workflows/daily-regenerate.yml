name: Daily CMMC Watch Regeneration

on:
  # Run daily at 6 AM EST (11 AM UTC)
  schedule:
    - cron: '0 11 * * *'

  # Run on push to main
  push:
    branches:
      - main

  # Allow manual trigger
  workflow_dispatch:
    inputs:
      skip_archive:
        description: 'Skip archiving previous website'
        required: false
        default: 'false'
        type: boolean

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write
  issues: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Restore previous build (for archiving)
        uses: actions/cache@v4
        with:
          path: |
            public/
            data/
          key: cmmcwatch-build-${{ github.run_number }}
          restore-keys: |
            cmmcwatch-build-

      - name: Clear today's archive to force fresh generation
        run: |
          TODAY=$(date +%Y-%m-%d)
          if [ -d "public/archive/$TODAY" ]; then
            echo "Removing existing archive for $TODAY"
            rm -rf "public/archive/$TODAY"
          fi
          # Also clear cached data to force fresh content
          rm -f data/*.json 2>/dev/null || true
          echo "Cleared stale data files"

      - name: Regenerate article HTML (apply latest navigation/footer)
        run: |
          cd scripts
          python editorial_generator.py --regenerate-html || echo "No articles to regenerate"

      - name: Run website generation pipeline
        env:
          GOOGLE_AI_API_KEY: ${{ secrets.GOOGLE_AI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          APIFY_API_KEY: ${{ secrets.APIFY_API_KEY }}
          APIFY_ACTOR_ID: ${{ secrets.APIFY_ACTOR_ID }}
        run: |
          cd scripts
          if [ "${{ github.event.inputs.skip_archive }}" = "true" ]; then
            python main.py --no-archive
          else
            python main.py
          fi

      - name: Cache build artifacts
        uses: actions/cache@v4
        with:
          path: |
            public/
            data/
          key: cmmcwatch-build-${{ github.run_number }}

      - name: Add CNAME for custom domain
        run: echo "cmmcwatch.com" > public/CNAME

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './public'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  notify-on-failure:
    runs-on: ubuntu-latest
    needs: [build, deploy]
    if: failure()

    steps:
      - name: Create issue on failure
        uses: actions/github-script@v7
        with:
          script: |
            const title = `CMMC Watch regeneration failed - ${new Date().toISOString().split('T')[0]}`;
            const body = `The daily CMMC Watch regeneration workflow failed.

            **Run:** [${context.runId}](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})
            **Time:** ${new Date().toISOString()}

            Please check the workflow logs for details.`;

            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'pipeline-failure'
            });

            const existingIssue = issues.data.find(issue =>
              issue.title.includes('CMMC Watch regeneration failed')
            );

            if (!existingIssue) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: title,
                body: body,
                labels: ['pipeline-failure', 'automated']
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existingIssue.number,
                body: `Pipeline failed again at ${new Date().toISOString()}\n\n[Run #${context.runId}](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`
              });
            }
